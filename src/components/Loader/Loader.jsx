import React from "react";
import { LoaderWrapper, LoaderIcon } from "./LoaderStyle";
const Loader = () => {
  return (
    <LoaderWrapper>
      <LoaderIcon />
    </LoaderWrapper>
  );
};

export default Loader;
